{
  "name": "@order-platform/shared-utils",
  "version": "0.1.0",
  "private": true,
  "description": "Zero-dependency shared utilities for the order platform",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@order-platform/shared-types": "workspace:*",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
